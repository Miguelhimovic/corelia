# Copiar a .env (fuera de Git, ver .gitignore) y completar con valores reales.
# SPEC.md seccion 8: secrets solo en variables de entorno, nunca en codigo ni en logs.

ENVIRONMENT=local

# Host 5433 -> evita choque con una instancia local de PostgreSQL en 5432.
# Ver docker-compose.yml (servicio "db").
DATABASE_URL=postgresql+psycopg2://corelia:corelia@localhost:5433/corelia

ANTHROPIC_API_KEY=
